# Progress Log

## Session 1 (Initializer) - 2026-08-22
- Created task list with 21 subtasks at `.autonomous/lie-kill-track-a-main/task_list.md`. Did not implement anything (per Initializer scope).
- **Key observation — the task description is stale relative to the live repo.** The `gitStatus` snapshot handed to this session showed `main` at commit `3de2371`, but `git log -1` shows the actual current HEAD is `59592c9` ("Note SPACE-001 verification in agent memory"), six commits ahead. Those six commits (`59592c9`, `15e650a`, `6748481`, `97999a2`, `287f45a`, `72e48b4`) already landed:
  - SPACE-001 (Actor/Workspace/Space/Membership schema + deny-by-default auth matrix) — verified, `IMPLEMENTATION_BACKLOG.md` status `verified`, evidence commit `6748481`, tests `space_authorization_matrix` in `mv-core` (5/5) and `mv-storage` (1/1).
  - SPACE-004 (communication-to-knowledge promotion boundary) — verified, evidence commit `e3010b1`. `AGENTS.md`'s SPACE-004 bullet, which the parent task described as a "stale live-violation claim," was already corrected in a prior commit (confirmed via `git log -p -- AGENTS.md`); it currently reads "SPACE-004 is verified (2026-07-31)...".
  - AGENT-001/002/003, WS-001/003/014, IK-002/IK-003 — all verified per `IMPLEMENTATION_BACKLOG.md` body text, none of which is reflected in the Dashboard table's per-group `verified` counts.
  - FED-000 (fail-closed federation, env-only gate `MINDVAULT_FEDERATION_ENABLED`, acceptance test `crates/mv-server/tests/federation_e2e.rs`) — already implemented and marked verified in the backlog, though its evidence line still says "Commit hash pending."
- **Real remaining gaps found (these became the task list):**
  1. `config/default.toml:109` has a dead `federation_enabled = false` key — confirmed via `grep -rn "federation_enabled" crates/` that no Rust config struct reads it; the actual runtime gate reads `MINDVAULT_FEDERATION_ENABLED` directly from the environment in `crates/mv-server/src/rest/federation.rs`.
  2. License surface has four genuine gaps: `README.md` has zero mentions of "license"; `frontend/src-tauri/Cargo.toml` still declares `license = "MIT"` despite the repo relicensing to proprietary (`ead3ec8`); `crates/mv-mcp/Cargo.toml` has no license field at all; `.gitignore` has no `.cursor/hooks/state/` entry, and that path's one file (`continual-learning.json`) is currently tracked and shows as modified in `git status`.
  3. `IMPLEMENTATION_BACKLOG.md`'s Dashboard (lines 97-115) is significantly stale — the Total row claims `9` verified items, but a loose scan for `**status:**` lines containing "verified" (accounting for a bold-markdown formatting inconsistency: some entries write `**status:** verified`, others `**status:** **verified** (date)`) found roughly 27 genuinely verified items. Spot-checked deltas: SPACE group verified 1→2, AGENT group verified 1→3, WS group verified 0→3, FED group verified 0→1.
  4. `CURRENT_SECURITY_AND_AUTHORITY_GAPS*` does not exist anywhere in the repo (`find . -iname "*SECURITY_AND_AUTHORITY*"` returned zero results) — the parent task's "sync if present" therefore requires no file action, just an explicit record of the absence.
  5. SPACE-002 (WorkOrder/AgentRun state machine) is marked `not_started` in the backlog but is **not** a from-scratch task: `crates/mv-core/src/model/work_order.rs` (1298 lines) already has `WorkOrderStatus`/`AgentRunStatus` with transition and retry-class logic, `WorkOrder`/`WorkOrderNode`/`WorkOrderEdge`/`AgentRun`/`WriteLease` structs; `crates/mv-engine/src/engine/work_order_ops.rs` (2603 lines) already has an extensive test module. What's actually missing: a test matching the backlog's exact acceptance command name (`work_order_agent_run`), self-approval/broaden-own-grant refusal logic (grepped for and found nowhere), and on-behalf-of principal/actor exposure. No `Artifact` struct exists anywhere in `crates/` — that concept belongs to SPACE-005 (P1, `blocked_by: SPACE-002`), not SPACE-002's actual acceptance text, so the task list explicitly scopes SPACE-002 to exclude Artifact modeling (see task list Note + subtask 14) to avoid scope creep into a different backlog item.
- **Architecture / sequencing decisions baked into the task list:**
  - Ordered the 21 subtasks so the "gate" work (FED-000, license, truth maintenance — items 1-3 of the parent task) all precede the SPACE-001 re-verification gate check (subtask 12), which itself precedes any SPACE-002 work (subtasks 13-18), matching the parent task's explicit "(5) Only after SPACE-001" constraint. Since SPACE-001 already landed, subtask 12 is a regression re-check, not new implementation, but it still gates SPACE-002 per the stated ordering.
  - SPACE-002 is scoped strictly to what `IMPLEMENTATION_BACKLOG.md`'s actual SPACE-002 entry requires (state machine + retries + self-approval refusal + on-behalf-of exposure), explicitly excluding Artifact modeling, which is SPACE-005's separate mandate. This decision is recorded as its own subtask (14) so a future session doesn't silently either skip or over-scope it.
  - No subtask instructs a git commit. The parent task says "do not commit unless task list explicitly requires it," and none of the five numbered items in the parent task description explicitly demand a commit — so the task list defaults to leaving work uncommitted, consistent with the project's git-safety norms (only commit when explicitly asked).
  - Every "truth maintenance" subtask requires evidence (command output + commit hash + date) before any status is changed, matching `IMPLEMENTATION_BACKLOG.md`'s own stated rule ("`verified` requires evidence... No command + commit + date → not verified").
- **Not done / deliberately deferred to the executing session (Initializer does not implement):** no code was written, no files were edited besides this task list and progress log, no tests were run to completion (only greps/finds/reads for orientation), no commits were made.

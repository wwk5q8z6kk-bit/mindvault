# Task: On branch main: execute the council hybrid Lie Kill half-day gate then Track A foundation. (1) FED-000 fail-closed federation (env-only gate, delete or document dead TOML federation_enabled, acceptance test). (2) Minimal license surface: README License section, Tauri MIT->license-file, mv-mcp license-file.workspace, gitignore .cursor/hooks/state/. (3) Truth maintenance: recount IMPLEMENTATION_BACKLOG dashboard from live verified statuses; fix AGENTS.md stale SPACE-004 live-violation claim; sync CURRENT_SECURITY_AND_AUTHORITY_GAPS if present. (4) SPACE-001 Actor/Workspace/Space/Membership schema + deny-by-default auth matrix with tests. (5) Only after SPACE-001: start SPACE-002 WorkOrder/AgentRun/Artifact SM acceptance. Do not enable FED-001..010 or live external effects. Evidence-first: mark backlog verified only with command+commit+date. Do not commit unless task list explicitly requires it.

## Subtasks
- [ ] 1. Baseline the session: record current HEAD commit hash (`git log -1 --format='%H %cI %s'`), run `cargo check --workspace` and record pass/fail, and confirm working tree state (`git status --short`). This is the evidence anchor every later "commit hash" citation in this session must match. NOTE: `.autonomous/lie-kill-track-a-main/` itself is currently untracked — do not let it block the check.
- [ ] 2. FED-000 acceptance re-verification: run `cargo test -p mv-server --test federation_e2e -- federation_disabled_by_default --test-threads=1` and confirm it currently passes on HEAD. Separately identify the actual commit that introduced the fail-closed `require_federation_enabled()` gate in `crates/mv-server/src/rest/federation.rs` and the acceptance test in `crates/mv-server/tests/federation_e2e.rs` (use `git log -p`/`git blame` on those files — this session found candidate commits `7baeeb1`/`6a11eaa`/`c98a130` touching those files but did not pin the exact one that added the current fail-closed behavior). Replace `IMPLEMENTATION_BACKLOG.md:814`'s "Commit hash pending" with the real hash + date.
- [ ] 3. FED-000 dead TOML key: `config/default.toml:109` sets `federation_enabled = false`, but no Rust config struct field reads it — the actual runtime gate is `std::env::var("MINDVAULT_FEDERATION_ENABLED")` read directly in `crates/mv-server/src/rest/federation.rs`, bypassing the TOML config system entirely (confirmed this session: `grep -rn "federation_enabled" crates/` only shows the function name and its call sites, no config-struct field). Pick one: (a) delete the dead key from `config/default.toml`, or (b) keep it but rewrite the surrounding comment to explicitly say it is inert/non-functional and the real gate is `MINDVAULT_FEDERATION_ENABLED` (env-only). Do not make the key functional (that would be a larger change beyond this gate's scope) and do not touch FED-001..010. Update the FED-000 entry's `files:` list in `IMPLEMENTATION_BACKLOG.md` if `config/default.toml` changes.
- [ ] 4. License surface — README: add a minimal `## License` section to `README.md` (currently has zero mentions of "license" — confirmed via `grep -ni license README.md`) linking to the `LICENSE` file. Do not paraphrase or invent terms beyond what `LICENSE` (MindVault Proprietary License, all-rights-reserved) already states.
- [ ] 5. License surface — Tauri crate: `frontend/src-tauri/Cargo.toml` currently declares `license = "MIT"`, which is stale since the repo relicensed to proprietary (`ead3ec8 chore: relicense to proprietary all-rights-reserved license`; root `Cargo.toml` workspace.package uses `license-file = "LICENSE"`). First check whether `frontend/src-tauri` is a member of the root `[workspace]` in `Cargo.toml` — if yes, use `license-file.workspace = true`; if no (likely, since it's a Tauri sub-crate), use a correct relative `license-file = "../../LICENSE"` (verify the relative path resolves) or `license-file = "LICENSE"` with a copy/symlink per Tauri convention — confirm whichever choice with `cargo metadata --no-deps` or `cargo check -p <tauri-crate-name>` before considering it done.
- [ ] 6. License surface — mv-mcp crate: `crates/mv-mcp/Cargo.toml` currently has no `license`/`license-file` field at all (confirmed via `grep -n license crates/mv-mcp/Cargo.toml`). First check the exact workspace-inheritance syntax used by a sibling crate (e.g. root `Cargo.toml` workspace.package block, and how another `crates/*/Cargo.toml` inherits it, if any already do), then add the matching `license-file.workspace = true` line to `crates/mv-mcp/Cargo.toml`. Verify with `cargo check -p mv-mcp`.
- [ ] 7. License surface — gitignore + untrack: add `.cursor/hooks/state/` to `.gitignore`. Note this alone is insufficient: `.cursor/hooks/state/continual-learning.json` is currently **tracked** (`git ls-files .cursor/hooks/state/` confirms it, and `git status --short` shows it modified) — a gitignore rule does not stop tracking of already-tracked files. Also run `git rm --cached .cursor/hooks/state/continual-learning.json` (keeps the file on disk, only removes it from the index) so the ignore rule actually takes effect going forward. Verify with `git status --short` no longer listing it, and `git check-ignore -v .cursor/hooks/state/continual-learning.json` confirming the new rule matches.
- [ ] 8. Truth maintenance — recount the `IMPLEMENTATION_BACKLOG.md` Dashboard (lines 97-115, header says "derived — recount, do not trust"). Method: for each group prefix (DOC, PROG, IK, EXT, PROTO, SRC, FED, PORT, SLICE, WS, SPACE, PARK, AGENT, HYG), enumerate every `#### <ID>` heading and its paired `- **status:**` line, counting `verified` regardless of bold-markdown variance (this session found both `**status:** verified` and `**status:** **verified** (...)` forms — a plain grep for the first form alone undercounts). This session's spot-check already found the Total row's `verified` column (currently `9`) is stale by roughly 3x — a loose scan found ~27 items with a `verified` status token, and confirmed at least these deltas versus the current table: SPACE row (currently `8|4|1`) should have verified=2 (SPACE-001 landed since the row was last derived, SPACE-004 already counted); AGENT row (currently `8|1|1`) should have verified=3 (AGENT-002, AGENT-003 also verified per body, only AGENT-001 was counted); WS row (currently `22|6|0`) should have verified=3 (WS-001, WS-003, WS-014 verified per body); FED row (currently `13|10|0`) should have verified=1 (FED-000 verified). Do a full recount from scratch (do not just apply these deltas blindly — they were a spot-check, not exhaustive) for every group's Items/P0/verified columns, replace the table, and do not change the "derived — recount, do not trust" header framing.
- [ ] 9. Truth maintenance — AGENTS.md SPACE-004 claim: verify (do not blindly re-fix) that `AGENTS.md`'s SPACE-004 bullet currently reads "SPACE-004 is verified (2026-07-31): relay messages stay out of the knowledge graph..." — this session confirmed via `git log -p -- AGENTS.md` that the old "SPACE-004 is a live P0... violates the promotion boundary" claim was already corrected in a prior commit. If the working tree at execution time still shows the corrected text, mark this subtask done as verification-only with the confirming command output. If it has somehow reverted, fix it to match `IMPLEMENTATION_BACKLOG.md`'s current SPACE-004 entry (status: verified, evidence commit `e3010b1`).
- [ ] 10. Truth maintenance — CURRENT_SECURITY_AND_AUTHORITY_GAPS: confirm the file does not exist anywhere in the repo (`find . -iname "*SECURITY_AND_AUTHORITY*" -not -path "./target/*"` — this session ran it and got zero results). Since it is absent, the parent task's "sync ... if present" requires no file edit; record this explicitly (command + zero-result output) in `progress.md` rather than silently skipping it, since "absent" is itself a verified fact worth citing.
- [ ] 11. Truth maintenance — incidental stale-evidence sweep: while doing subtask 8's recount, grep `IMPLEMENTATION_BACKLOG.md` for other `evidence: —` entries paired with `status: verified` (contradiction — should never happen per the file's own status vocabulary) and for other "pending"/TBD-style evidence strings besides the FED-000 one already fixed in subtask 2. File each as a small, evidence-cited correction (command + commit + date) — do not invent or upgrade a status without a passing acceptance command backing it.
- [ ] 12. Gate check before SPACE-002: re-run SPACE-001's cited acceptance commands on current HEAD to confirm they still pass as a regression check (this is verification, not re-implementation — SPACE-001 schema + deny-by-default auth matrix already landed in commits `6748481`/`15e650a`/`59592c9`): `cargo test -p mv-core -- space_authorization_matrix` and `cargo test -p mv-storage -- space_authorization_matrix`. Record the pass counts, current HEAD hash, and date. If either fails, stop and treat the failure as the next task (fix the regression) instead of proceeding to subtask 13 — per the parent task's explicit ordering constraint ("Only after SPACE-001").
- [ ] 13. SPACE-002 inventory: read `docs/architecture/collaborative-spaces-baseline.md` lines ~89-93 (P0 "attribution and delegated authority" unlock evidence) and `docs/adr/010-personal-vault-and-collaborative-spaces.md` lines ~121-134, then read `crates/mv-core/src/model/work_order.rs` (1298 lines — already has `WorkOrderStatus`, `AgentRunStatus` with `can_transition_to`/`is_terminal`, `WorkOrder`, `WorkOrderNode`, `WorkOrderEdge`, `AgentRun`, `WriteLease`, budget/spend types) and `crates/mv-engine/src/engine/work_order_ops.rs` (2603 lines, existing `mod tests` from line ~1220) in full. Produce a written inventory in `progress.md` of exactly what state-machine/transition/retry coverage already exists versus what `IMPLEMENTATION_BACKLOG.md`'s SPACE-002 acceptance bar (`cargo test -- work_order_agent_run`) still requires, before writing any new code — substantial scaffolding already exists and the primary risk is duplicating rather than extending it.
- [ ] 14. SPACE-002 scope decision: the parent task's item (5) title says "WorkOrder/AgentRun/Artifact SM acceptance," but `IMPLEMENTATION_BACKLOG.md`'s actual SPACE-002 entry acceptance text (state machines/retries/self-approval/on-behalf-of) does not mention Artifact — versioned-artifact modeling is SPACE-005's mandate (P1, `blocked_by: SPACE-002`, currently `status: not_started`, no `Artifact` struct exists anywhere in `crates/` — confirmed via repo-wide grep this session). Default decision absent contrary instruction: scope this session's SPACE-002 work strictly to WorkOrder/AgentRun state machine + retry + self-approval-refusal + on-behalf-of exposure (matching the backlog's actual acceptance text), and explicitly leave Artifact modeling out of scope for a future SPACE-005 task rather than scope-creeping SPACE-002. Record this decision and its rationale in `progress.md`.
- [ ] 15. SPACE-002 state-machine + retry acceptance tests: using the existing `WorkOrderStatus::can_transition_to` / `AgentRunStatus::can_transition_to` machinery in `crates/mv-core/src/model/work_order.rs`, add or complete tests in `crates/mv-engine` (or wherever the inventory from subtask 13 shows the gap) covering the state-machine transitions and retry behavior (`RunFailureClass::permits_identical_retry` already exists — verify it's exercised by a test), such that a test target discoverable via `cargo test -- work_order_agent_run` exists and passes (the backlog's exact acceptance command — no such test currently exists under that name, confirmed via repo-wide grep this session).
- [ ] 16. SPACE-002 self-approval refusal: implement "no action can approve or broaden its own grant" (quoted directly from the SPACE-002 mandate and ADR 010) wherever WorkOrder/AgentRun approval transitions are gated (likely `crates/mv-core/src/model/work_order.rs`, `crates/mv-engine/src/engine/work_order_ops.rs`, and/or `crates/mv-server/src/rest/work_orders.rs` — confirmed this session via grep that no self-approval/broaden-own-grant refusal logic currently exists anywhere in these files). Add a unit or integration test proving a run cannot approve or broaden its own grant.
- [ ] 17. SPACE-002 on-behalf-of exposure: implement/confirm "on-behalf-of messages expose both principal and acting actor" (quoted from the SPACE-002 mandate) for WorkOrder/AgentRun paths, with a test asserting both fields are present, populated, and distinguishable in the relevant model/API surface.
- [ ] 18. SPACE-002 acceptance run + backlog update: run `cargo test -- work_order_agent_run` plus a broader regression sweep (`cargo test -p mv-core -p mv-engine -p mv-server`), capture pass/fail output. Update `IMPLEMENTATION_BACKLOG.md`'s SPACE-002 entry status strictly per the file's own status vocabulary (`not_started`/`in_progress`/`verified` — never self-assert `verified` without a passing acceptance command as evidence): if the acceptance command fully passes, flip to `verified` with `evidence:` naming the exact command, pass count, commit hash, and date; if only partial coverage lands this session, leave it `in_progress` and describe precisely what's still missing so a future session doesn't have to rediscover it.
- [ ] 19. Closeout regression sweep: run `cargo check --workspace` and the specific suites touched this session (`federation_e2e`, `space_authorization_matrix`, `work_order_agent_run`, plus `cargo check -p mv-mcp` and the Tauri manifest check from subtask 5) to confirm nothing regressed relative to the subtask 1 baseline. Assemble a single consolidated evidence table (command, result, commit hash, date) in `progress.md` covering every item touched this session (FED-000, all four license items, the dashboard recount, SPACE-001 reverification, SPACE-002).
- [ ] 20. AGENTS.md learned-facts update: append small, factual, evidence-cited bullets to the "Learned Workspace Facts" section reflecting this session's real outcomes (SPACE-002 status change if any, FED-000 dead-TOML resolution, license-surface completion) in the file's existing terse style. Do not rewrite or remove unrelated existing bullets.
- [ ] 21. Final consistency check: re-run the subtask 8 dashboard recount method one more time against the post-session state of `IMPLEMENTATION_BACKLOG.md` (after SPACE-002's status flip and any subtask-11 corrections) to confirm the table you wrote is still accurate, and note in `progress.md` that a future session should re-run the recount command rather than trust the table indefinitely — matching the file's own "derived — recount, do not trust" framing.

## Notes
- **Repo state has moved since the task was framed.** Recent commits on `main` (`6748481`, `15e650a`, `59592c9`, `97999a2`, `287f45a`, `72e48b4`, `3de2371`, `ebf61a8`, `ae74766`, `49fb8f7`) already landed SPACE-001 (verified), SPACE-004 (verified), AGENT-001/002/003 (verified), IK-002/IK-003 (verified), WS-001/003/014 (verified), and FED-000 (verified). The AGENTS.md "stale SPACE-004 live-violation claim" from the parent task description is **already fixed** in the working tree — subtask 9 is a verification, not a rewrite. Do not redo any of SPACE-001; only re-verify it (subtask 12) before starting SPACE-002.
- **"Lie Kill" is process framing, not a repo artifact** — `grep -rn "Lie Kill"` across the repo returns zero hits. Treat items (1)-(3) as the fast, evidence-focused "kill false/stale claims" gate; item (4) is a verification-only gate check (already implemented); item (5) is the substantive Track A build work.
- **Constraints carried through from the parent task, binding on every subtask above:** do not enable FED-001..FED-010; do not add live external network effects (no real federation transport, no live adapter delivery); every `status: verified` in `IMPLEMENTATION_BACKLOG.md` requires a named command + commit hash + date as evidence (the file's own rule, section "Status vocabulary" / "Rules that keep this file honest"); do not create a git commit unless a subtask explicitly requires one — none of the subtasks above are written as "commit and push," so default to leaving changes uncommitted in the working tree unless a later session is explicitly told otherwise.
- **SPACE-002 is far less "not_started" than its backlog status implies.** `crates/mv-core/src/model/work_order.rs` (1298 lines) and `crates/mv-engine/src/engine/work_order_ops.rs` (2603 lines, with an existing test module) already contain most of the WorkOrder/AgentRun state-machine scaffolding. The real gaps found this session are: no test named/matching `work_order_agent_run`, no self-approval/broaden-own-grant refusal logic, and no `Artifact` struct anywhere (which is out of SPACE-002's actual scope per subtask 14's analysis — it belongs to SPACE-005).
- **License-file syntax must be confirmed against actual sibling-crate convention before applying**, not assumed — subtasks 5 and 6 both say "check first, then apply," because this repo's workspace-inheritance pattern for `license-file` was not fully enumerated during initialization (only the root `Cargo.toml`'s `license-file = "LICENSE"` was confirmed).
- **`.cursor/hooks/state/continual-learning.json` is currently tracked and modified** (visible in the original `git status` snapshot as `M .cursor/hooks/state/continual-learning.json`) — this is unrelated session/hook churn, not user work in progress; subtask 7 explicitly untracks it via `git rm --cached`, not `rm`, so the file stays on disk.
- Evidence commands recorded during initialization (re-usable, do not re-derive from scratch): `git log -1 --format='%H %cI %s'` → `59592c9 2026-07-31T15:53:57-04:00 Note SPACE-001 verification in agent memory.`; `grep -c "^\- \*\*status:\*\* verified" IMPLEMENTATION_BACKLOG.md` → `18` (plain form only, undercounts due to bold-variant `**verified**` formatting — see subtask 8).

## Learned User Preferences

- Treat discussed product examples as conformance floors, not the product ceiling; expand via competitor and market research when enhancing.
- After strategy and research direction are set, prefer implementation and coding over further plan-only expansion.
- Use evidence-first verification: presence of code, schemas, routes, or docs is not proof of production-ready implementation.
- When executing an attached implementation plan, do not edit the plan file itself.

## Learned Workspace Facts

- Product model is Private Personal Vaults plus Governed Shared Spaces, not a single-owner-only vault.
- Primary commercial wedge is Trusted Agent Work for AI-native knowledge teams (`docs/strategy/unicorn/PRIMARY_WEDGE_DECISION.md`).
- Unicorn strategy pack lives under `docs/strategy/unicorn/`; `IMPLEMENTATION_BACKLOG.md` is the evidence-gated P0–P3 execution tracker.
- Repository packaging uses proprietary `LICENSE` via `license-file` (not Apache-2.0).
- Interoperability constitution and authority/grant model docs already exist under `docs/architecture/`.

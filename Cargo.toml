[workspace]
resolver = "2"
members = [
    "crates/mv-core",
    "crates/mv-storage",
    "crates/mv-index",
    "crates/mv-graph",
    "crates/mv-engine",
    "crates/mv-server",
    "crates/mv-mcp",
    "crates/mv-cli",
    "crates/mv-plugin",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["MindVault Contributors"]

[workspace.dependencies]
# Internal crates
mv-core = { path = "crates/mv-core" }
mv-storage = { path = "crates/mv-storage" }
mv-index = { path = "crates/mv-index" }
mv-graph = { path = "crates/mv-graph" }
mv-engine = { path = "crates/mv-engine" }
mv-server = { path = "crates/mv-server" }
mv-mcp = { path = "crates/mv-mcp" }
mv-plugin = { path = "crates/mv-plugin" }

# Async runtime
tokio = { version = "1", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Web / API
axum = { version = "0.7", features = ["ws", "multipart"] }
tonic = "0.12"
prost = "0.13"
tower = "0.4"
tower-http = { version = "0.5", features = ["cors", "trace"] }
hyper = "1"
hyper-util = { version = "0.1", features = ["tokio"] }

# Storage
rusqlite = { version = "0.31", features = ["bundled", "vtab"] }
lancedb = "0.13"
arrow-array = "52"
arrow-schema = "52"

# Search
tantivy = "0.22"

# Graph
petgraph = "0.6"

# IDs & time
uuid = { version = "1", features = ["v5", "v7", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# CLI
clap = { version = "4", features = ["derive"] }

# Config
figment = { version = "0.10", features = ["toml", "env"] }
toml = "0.8"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Error handling
thiserror = "2"
anyhow = "1"

# Auth
jsonwebtoken = "9"

# HTTP client (for embeddings API)
reqwest = { version = "0.12", features = ["json", "multipart"] }

# System / low-level
libc = "0.2"

# URL parsing
url = "2"

# Misc
async-trait = "0.1"
futures = "0.3"
bytes = "1"
tokio-stream = "0.1"

# OpenAPI
utoipa = { version = "5", features = ["axum_extras", "chrono", "uuid"] }
utoipa-swagger-ui = { version = "8", features = ["axum"] }

# Secret sharing
sharks = "0.5"

# Encryption
aes-gcm = "0.10"
argon2 = "0.5"
rand = "0.8"
zeroize = { version = "1", features = ["derive"] }
base64 = "0.22"
hkdf = "0.12"
hmac = "0.12"
sha2 = "0.10"

# WASM plugin runtime
wasmtime = "27"
wasmtime-wasi = "27"

[profile.release]
lto = "thin"
strip = true

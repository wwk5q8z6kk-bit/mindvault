# Audit Evidence

This directory contains machine-readable evidence captured against Git commit
`dcd7dbdb8c87ca01e22777f79e8f196b993fca23` on 2026-07-25. It contains no intentionally copied
credential value or user content.

The Cargo metadata snapshots normalize the audit checkout and Cargo cache prefixes to
`<REPOSITORY_ROOT>` and `<CARGO_HOME>`. Package identities, dependency edges, feature resolution,
and license fields are otherwise unchanged.

| Artifact | Purpose | Important limit |
|---|---|---|
| `SBOM.cdx.json` | CycloneDX 1.6 SBOM generated by Trivy 0.70.0; 1,843 components | Component discovery is not legal clearance or a vulnerability result |
| `evidence/cargo-metadata.json` | Full Rust workspace/dependency resolution | License declarations may be missing or inaccurate |
| `evidence/tauri-cargo-metadata.json` | Tauri package dependency resolution | Transitive inventory only |
| `evidence/trivy-licenses.json` | License scanner output | Requires human review of exceptions and notices |
| `evidence/trivy-vulnerabilities.json` | Timestamped vulnerability scan of seven lockfile ecosystems | 217 occurrences / 203 unique IDs; critical/high reachability is dispositioned in [`VULNERABILITY_REACHABILITY_AND_DISPOSITION.md`](../VULNERABILITY_REACHABILITY_AND_DISPOSITION.md), but findings remain unresolved |
| `evidence/gitleaks-history.json` | Full-history secret-scan findings | Two documented `demo-token` examples require allowlist/disposition |
| `evidence/gitleaks-working-tree.json` | Tracked working-tree-diff secret scan | An untracked-file scan is still required immediately before publication |
| `evidence/gitleaks-untracked.json` | Concatenated-content scan of the 20 untracked files visible at capture recheck | Paths are not retained in stdin mode; repeat after any local change |

The vulnerability result includes 3 critical, 56 high, 114 medium, and 44 low occurrences.
Development/test dependencies were suppressed by Trivy's default, and installed build/dependency
trees were excluded. Do not interpret scanner severity as proof of reachability or the SBOM/license
report as release clearance.

Before relying on these files, verify them against the audit package manifest created by
[BACKUP_AND_RESTORE_PROCEDURE.md](../BACKUP_AND_RESTORE_PROCEDURE.md). Regenerate all evidence
after dependency, lockfile, submodule, source, or build-input changes.

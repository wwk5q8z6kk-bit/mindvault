[package]
name = "hello-world-plugin"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Example MindVault WASM plugin"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
